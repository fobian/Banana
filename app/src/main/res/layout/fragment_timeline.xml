<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clipChildren="false"
    tools:context=".ui.TimeLineFragment">

    <com.chijsh.banana.widget.MultiSwipeRefreshLayout
        android:id="@+id/swip_refresh_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipToPadding="false"
        >

        <com.chijsh.banana.widget.observablerecyclerview.ObservableRecyclerView
            android:id="@+id/time_line"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:paddingTop="?actionBarSize"
            android:clipToPadding="false"
            android:scrollbars="vertical" />
    </com.chijsh.banana.widget.MultiSwipeRefreshLayout>

    <ImageButton
        android:id="@+id/fab"
        android:layout_width="@dimen/fab_size"
        android:layout_height="@dimen/fab_size"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_marginBottom="@dimen/fab_margins"
        android:layout_marginRight="@dimen/fab_margins"
        android:background="@drawable/fab_button"
        android:src="@drawable/ic_mode_edit_white_24dp"
        android:elevation="@dimen/fab_elevation"
        />


</RelativeLayout>
